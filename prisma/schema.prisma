generator client {
  provider = "prisma-client-js"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}


datasource db {
  provider = "postgresql" // e.g. "sqlite", "mysql", "sqlserver", "mongodb"
  url      = env("DATABASE_URL")
}
